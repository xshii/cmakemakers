# CMakeMakers

一个用于可视化编辑和生成 CMakeLists.txt 的 VSCode 插件。通过直观的表格界面管理 CMake 项目配置，无需记忆复杂的 CMake 语法。

## 🎯 项目愿景

将 CMake 项目配置从命令式脚本转变为声明式数据配置，让开发者专注于项目结构本身，而非 CMake 语法细节。

## ✨ 核心特性

### 📦 Target 管理
- 支持所有 CMake Target 类型（Executable、Static/Shared Library、Interface Library 等）
- 表格化展示和编辑 Target 属性
- 可视化依赖关系管理
- 循环依赖检测

### 📁 智能源文件管理
- **四种添加方式**：
  - 按目录扫描（支持递归）
  - Glob 模式匹配
  - 文件/文件夹名称规则
  - 手动文件选择
- 实时预览匹配结果
- 灵活的文件和文件夹过滤器
- 自动排除测试/备份文件

### 🔗 依赖管理
- 系统库链接（pthread、m、dl）
- find_package 可视化配置
- 内部 Target 依赖
- FetchContent 远程依赖
- Scope 管理（PUBLIC/PRIVATE/INTERFACE）

### 🛠️ 工具链支持
- 内置常用工具链模板（ARM、RISC-V、MinGW）
- 可视化配置交叉编译参数
- 自定义工具链模板保存和分享

### 📝 配置存储
- **YAML 配置文件**（`cmake/cmaker_config.yaml`）
  - 人类可读可编辑
  - 版本控制友好
  - 易于分享和复用
- **自动生成 CMakeLists.txt**
  - 格式化良好
  - 保持注释
  - 遵循最佳实践

### ⚙️ 编译选项管理
- 编译定义（Compile Definitions）
- 编译选项（Compile Options）
- 链接选项（Link Options）
- 平台条件配置支持

## 🏗️ 技术架构

### 整体架构

```
┌─────────────────────────────────────────────────┐
│            VSCode Extension Host                 │
├─────────────────────────────────────────────────┤
│  ┌──────────────┐      ┌──────────────┐        │
│  │   Webview    │◄────►│  Extension   │        │
│  │   (React)    │      │   Provider   │        │
│  └──────────────┘      └──────────────┘        │
│         │                      │                │
│         │              ┌──────────────┐        │
│         │              │ CMake Parser │        │
│         │              │  & Generator │        │
│         │              └──────────────┘        │
│         │                      │                │
│         └──────────────────────┼────────────┐  │
│                                ▼            │  │
│                        ┌──────────────┐    │  │
│                        │  Data Model  │    │  │
│                        └──────────────┘    │  │
│                                │            │  │
│                                ▼            ▼  │
│                    ┌──────────────────────────┐│
│                    │ YAML ⇄ CMakeLists.txt    ││
│                    └──────────────────────────┘│
└─────────────────────────────────────────────────┘
```

### 技术栈

#### Frontend (Webview UI)
- **框架**: React + TypeScript
- **状态管理**: Zustand / React Context
- **UI 组件**: VSCode Webview UI Toolkit
- **表单处理**: React Hook Form
- **构建工具**: Webpack

#### Backend (Extension)
- **语言**: TypeScript
- **VSCode API**: Extension API
- **YAML 处理**: js-yaml
- **文件匹配**: minimatch / glob
- **文件监听**: VSCode FileSystemWatcher

## 📂 项目结构

```
cmakemakers/
├── package.json                 # VSCode 插件配置
├── tsconfig.json
├── README.md                    # 本文件
├── DESIGN.md                    # 详细设计文档
├── src/                         # Extension 后端代码
│   ├── extension.ts             # 插件入口
│   ├── providers/
│   │   ├── CMakeEditorProvider.ts
│   │   └── TreeDataProvider.ts
│   ├── core/
│   │   ├── parser/              # CMake 解析器
│   │   │   ├── CMakeParser.ts
│   │   │   └── YAMLParser.ts
│   │   ├── generator/           # CMake 生成器
│   │   │   ├── CMakeGenerator.ts
│   │   │   └── YAMLSerializer.ts
│   │   └── model/               # 数据模型
│   │       ├── Project.ts
│   │       ├── Target.ts
│   │       └── Toolchain.ts
│   ├── services/
│   │   ├── FileSystemService.ts
│   │   ├── SourceFileScanner.ts
│   │   ├── ToolchainService.ts
│   │   └── ValidationService.ts
│   └── utils/
│       ├── logger.ts
│       └── validators.ts
├── webview/                     # React UI
│   ├── src/
│   │   ├── App.tsx
│   │   ├── components/
│   │   │   ├── ProjectSettings.tsx
│   │   │   ├── TargetTable.tsx
│   │   │   ├── SourceFileManager.tsx
│   │   │   ├── DependencyMatrix.tsx
│   │   │   └── ToolchainConfig.tsx
│   │   ├── hooks/
│   │   └── types/
│   ├── package.json
│   └── webpack.config.js
├── templates/                   # 项目模板
│   ├── console-app.yaml
│   ├── static-lib.yaml
│   └── shared-lib.yaml
└── docs/                        # 文档
    ├── user-guide.md
    └── api.md
```

## 🎨 用户界面设计

### 主界面（Tab-based）

```
┌─────────────────────────────────────────────────────────────┐
│  CMakeMakers - Project: MyApp                    [Generate] │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  [📋 Project] [🎯 Targets] [🔗 Dependencies] [⚙️ Toolchain] │
│                                                               │
│  ┌─────────────────────────────────────────────────────────┐│
│  │              表格化编辑界面（见 DESIGN.md）               ││
│  └─────────────────────────────────────────────────────────┘│
│                                                               │
│  [Preview CMakeLists.txt]                                    │
└─────────────────────────────────────────────────────────────┘
```

### Target 表格示例

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  Targets                                              [+ Add Target]         │
├──────┬──────────────┬──────────────┬────────────────┬─────────────┬─────────┤
│ Type │ Name         │ Sources      │ Include Dirs   │ Link Libs   │ Actions │
├──────┼──────────────┼──────────────┼────────────────┼─────────────┼─────────┤
│ 📦   │ my_app       │ 3 files ✏️   │ 2 dirs ✏️      │ 2 libs ✏️   │ ✏️ 🗑️  │
│ 📚   │ mylib        │ 5 files ✏️   │ 1 dir ✏️       │ 1 lib ✏️    │ ✏️ 🗑️  │
└──────┴──────────────┴──────────────┴────────────────┴─────────────┴─────────┘
```

## 🚀 开发路线图

### Phase 0: MVP（当前阶段）
- [x] 架构设计
- [x] 功能规划
- [ ] 基础项目结构
- [ ] Target CRUD（表格）
- [ ] 源文件管理（文件选择器）
- [ ] Include 目录管理
- [ ] 链接库管理（基础）
- [ ] YAML 配置存储
- [ ] CMakeLists.txt 生成器

**预计时间**: 2 周

### Phase 1: 核心功能
- [ ] 完整的 Target 类型支持
- [ ] 源文件智能添加（目录扫描、Glob）
- [ ] find_package 可视化配置
- [ ] 编译选项管理
- [ ] 工具链基础配置
- [ ] 实时预览
- [ ] 配置验证
- [ ] 从 CMakeLists.txt 导入

**预计时间**: 3 周

### Phase 2: 增强功能
- [ ] 依赖关系可视化
- [ ] 项目模板系统
- [ ] CMakePresets 集成
- [ ] FetchContent 支持
- [ ] 安装配置
- [ ] 测试集成
- [ ] 高级工具链管理

**预计时间**: 4 周

### Phase 3: 高级功能
- [ ] 多项目管理（add_subdirectory）
- [ ] 条件编译可视化
- [ ] 依赖分析
- [ ] 性能优化建议
- [ ] 插件 API
- [ ] 国际化（中文/英文）

**预计时间**: 持续迭代

## 📖 配置文件示例

### cmaker_config.yaml

```yaml
project:
  name: MyAwesomeProject
  version: 1.0.0
  cmake_minimum_required: "3.15"
  languages:
    - CXX

global:
  cxx_standard: 17
  cxx_standard_required: true
  default_build_type: Release

targets:
  - name: my_app
    type: executable
    sources:
      - pattern: src/**/*.cpp
        type: glob
        recursive: true
        exclude:
          - "**/*_test.cpp"

    include_directories:
      - path: include
        scope: PUBLIC
      - path: src
        scope: PRIVATE

    link_libraries:
      - name: pthread
        scope: PRIVATE
      - name: Boost::system
        scope: PUBLIC
        find_package: Boost
        version: "1.70"
        components: [system, filesystem]

toolchain:
  preset: native

metadata:
  generated_by: CMakeMakers
  version: 1.0.0
```

## 🔧 开发指南

### 前置要求

- Node.js >= 16
- VSCode >= 1.80
- TypeScript >= 5.0

### 安装依赖

```bash
# 主项目依赖
npm install

# Webview 依赖
cd webview
npm install
```

### 开发

```bash
# 编译 Extension
npm run compile

# 监听模式
npm run watch

# 编译 Webview
cd webview
npm run build
npm run watch
```

### 调试

1. 按 F5 启动 Extension Development Host
2. 在新窗口中打开测试项目
3. 使用命令面板：`CMakeMakers: Open Config Editor`

### 测试

```bash
# 运行单元测试
npm test

# 运行集成测试
npm run test:integration
```

## 🤝 贡献指南

欢迎贡献！请查看 [CONTRIBUTING.md](CONTRIBUTING.md)

### 开发流程

1. Fork 项目
2. 创建功能分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 创建 Pull Request

## 📄 许可证

本项目采用 Apache License 2.0 许可证 - 详见 [LICENSE](LICENSE) 文件

## 🙏 致谢

- VSCode Extension API
- CMake 社区
- 所有贡献者

## 📮 联系方式

- 问题反馈: [GitHub Issues](https://github.com/xshii/cmakemakers/issues)
- 功能建议: [GitHub Discussions](https://github.com/xshii/cmakemakers/discussions)

---

**注意**: 本项目目前处于设计阶段，核心功能正在开发中。欢迎提前关注和参与讨论！
